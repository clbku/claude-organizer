{
  "mcpServers": {
    "claude-organizer": {
      "type": "http",
      "url": "${CO_MCP_URL:-http://localhost:4402/mcp}",
      "headers": {
        "Authorization": "Bearer ${CO_MCP_TOKEN:-}"
      }
    }
  }
}
